<div class="card {{if faceDown "face-down" ""}}"
  data-test="card">
  {{#if faceUp}}
    <p class="card-title"
      data-test="card-title">
      {{card.title}}
    </p>
    <img
      class="card-image"
      data-test="card-image"
      src={{card.image}}
      alt={{card.name}} >
    <p class="card-description"
      data-test="card-description">
      {{card.description}}
    </p>
  {{/if}}
</div>
